name: druginsight
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - rdkit
  - pip
  - pip:
      - streamlit
      - torch
      - torch-geometric
      - numpy
      - pandas
      - scikit-learn
